# Frontend Environment Variables
# Copy this file to .env and update the values

# API Base URL for Azure Functions backend
REACT_APP_API_BASE_URL=https://inez-ai-function.azurewebsites.net/api

# Backend Environment Variables (set in Azure Function App Configuration)
# These are set in Azure Portal > Function App > Configuration > Application settings

# JWT Secret for token generation (generate a secure random string)
# JWT_SECRET=your-secure-jwt-secret-here

# SMTP Configuration for email verification
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password

# Optional: Custom domain (if using custom domain)
# REACT_APP_CUSTOM_DOMAIN=https://yourdomain.com 